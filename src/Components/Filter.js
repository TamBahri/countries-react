import React from "react";

const Filter = () => {
  return (
    <div className="filter">
      <select>
        <option>
          {/* <option selected disabled hidden> */}
          Filter by Origin
        </option>
      </select>
    </div>
  );
};

export default Filter;
